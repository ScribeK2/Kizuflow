<div class="page-content page-narrow">
  <div class="page-header-section">
    <%= render "admin/breadcrumb", trail: admin_group_trail(@group.parent, include_self: true),
          current: @group.parent ? "New subgroup" : "New group" %>
    <h1 class="page-header-section__title">New Group</h1>
  </div>
  <%= render "form", group: @group, parent_options: @parent_options %>
</div>
