service: turboflows

image: your-registry/turboflows

servers:
  web:
    hosts:
      - your-server.com
    cmd: bin/rails server

env:
  secret:
    - RAILS_MASTER_KEY
    - DATABASE_PASSWORD

volumes:
  - storage:/rails/storage

